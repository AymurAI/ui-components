node_modules
dist
storybook-static

# Panda generated output (regenerated by `panda codegen`)
src/styled

# Logs / OS
*.log
.DS_Store
*.tgz

# Figma fidelity audit scratch (playwright + screenshots)
.audit/

# Local-only superpowers specs and plans (never committed)
docs/superpowers/
