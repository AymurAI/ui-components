# Changesets

This folder is managed by [changesets](https://github.com/changesets/changesets).

Run `pnpm changeset` to record a change and choose the version bump. On merge to
`main`, CI consumes the accumulated changesets to version and publish
`@aymurai/ui`.
