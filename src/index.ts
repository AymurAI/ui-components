/**
 * @aymurai/ui — public API.
 *
 * Components are re-exported here as they are implemented. Consumers import the
 * components from the package root and the stylesheet once:
 *
 *   import { Button } from "@aymurai/ui";
 *   import "@aymurai/ui/styles.css";
 */
export { Button, type ButtonProps } from "./components/button";
